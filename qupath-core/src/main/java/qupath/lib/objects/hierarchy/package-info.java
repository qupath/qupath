/**
 * Classes to represent the {@linkplain qupath.lib.objects.hierarchy.PathObjectHierarchy object hierarchy}.
 * <p>
 * A hierarchy exists for each {@link qupath.lib.images.ImageData ImageData} and manages the relationships between 
 * objects.
 * <p>
 * This package also provides separate classes to managing TMA core objects specifically.
 */
package qupath.lib.objects.hierarchy;